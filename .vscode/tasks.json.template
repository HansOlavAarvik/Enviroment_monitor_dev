{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake -B build -G Ninja && cmake --build build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}